# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Swiss Ephemeris data files (downloaded via `pnpm ephe:install`)
ephe/

# Local runtime artifacts
web/last-result.json
*.local

# Browser engine bundle + wasm assets (built by `pnpm build:web`; shipped in
# the npm tarball via package.json "files", but not committed)
web/engine.js
web/swisseph.wasm
web/swisseph.data

# Editor / tooling
.claude/
.vscode/
.idea/

# Internal development planning docs (not part of the shipped project)
docs/superpowers/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Packed tarballs (pnpm pack)
*.tgz
